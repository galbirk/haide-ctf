{{- $root := . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.TwentyFortyEight.name }}
spec:
  replicas: {{ .Values.TwentyFortyEight.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: {{ .Values.TwentyFortyEight.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.TwentyFortyEight.name }}
    spec:
      restartPolicy: {{ .Values.TwentyFortyEight.restartPolicy }}
      containers:
    {{- range untilStep 10 13 1 }}
        - name: {{ $root.Values.TwentyFortyEight.name }}-{{ . }}
          image: {{ $root.Values.TwentyFortyEight.image }}
          resources:
            limits:
              memory: "128Mi"
              cpu: "250m"
          env:
            - name: APACHE_PORT
              value: "80{{ . }}"
            - name: SSH_PORT
              value: "22{{ . }}"
          ports:
            - containerPort: 80{{ . }}
            - containerPort: 22{{ . }}
          imagePullPolicy: Always
    {{- end }}